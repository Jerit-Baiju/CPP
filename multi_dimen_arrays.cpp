#include <stdio.h>
#include <iostream>
using namespace std;

int main()
{
    string letters[2][4] = {
        {"A", "B", "C", "D"},
        {"E", "F", "G", "H"}};
    cout << letters[0][3];

    // cout << sizeof(letters);
    // for(int i=0; i<sizeof)

    return 0;
}
